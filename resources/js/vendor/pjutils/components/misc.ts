export function loader(): string {
    let loader = '<div class="loader">';
    loader += '<div class="loading-dot"></div>';
    loader += '<div class="loading-dot"></div>';
    loader += '<div class="loading-dot"></div>';
    loader += '</div>';

    return loader;
}